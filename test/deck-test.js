const chai = require('chai');
const expect = chai.expect;

const {} = require('../src/deck');

describe('createDeck', function() {
  it ('should create a deck with exactly 3 cards', function() {
    const card1 = createCard(3, 'What type of prototype method directly modifies the existing array?')
  })
})